{
  "name": "@nest-base/cli",
  "version": "1.1.0",
  "description": "",
  "main": "dist/src/index.js",
  "bin": {
    "nest-base": "bin/nest-base-cli"
  },
  "files": [
    "bin",
    "templates",
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist/* && tsc",
    "lint:commit": "commitlint --from=HEAD~1 --to=HEAD"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "husky": "^9.1.7"
  },
  "author": "",
  "license": "ISC"
}
